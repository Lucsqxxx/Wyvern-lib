# Azure → Wyvern UI Inventory (UI-only)

Source: Azure deobf succes AgentX77.txt

## Tabs
- Main
- Blatant
- Spam
- Detection
- Player
- Visual
- Misc
- World
- GUI
- Unlock

## Modules / Features (labels only)
- **Theme Editor** (`ThemeEditor`) — right — Customize the interface theme
- **Unlock All** (`UnlockAll`) — left — Unlock all Swords, Explosions and Emotes
- **GUI Visible** (`guilibraryvisible`) — left — Visibility of GUI Library
- **FPS and Ping** (`StatsOverlayModule`) — right — Show your FPS and Ping
- **Infinite Jump** (`infinitejump`) — left — Character Infinite Jump
- **FPS Booster** (`FPSBooster`) — right — Lower your Graphics to improve FPS
- **Low Graphics** (`LowGraphics`) — right — Reduce Rendering quality and shadows
- **Auto Play** (`AutoPlay`) — right — Automatically Plays Game
- **Ball Trail** (`Ball_Trail`) — left — Toggles ball trail effects
- **Feature** (``) — left — Toggles custom world filter effects
- **Feature** (`sound_controller`) — left — Control background music and sounds
- **Auto Parry** (`AutoParryModule`) — left — Auto Parry Settings
- **Mobile Curve Selector** (`SetCurveModule`) — right — Curve Mode Selector in Mobile
- **Humanizer** (`HumanizerModule`) — right — Choose a random parry accuracy range.
- **Staff Detection** (``) — left — Detect Bladeball Mod in the server
- **Infinity Detection** (`InfinityModule`) — left — Detect infinity balls
- **Death Slash Detection** (`DeathSlashModule`) — right — Detect death slash
- **Time Hole Detection** (`TimeHoleModule`) — left — Detect time hole
- **Slashes Of Fury Detection** (`SlashesModule`) — right — Detect slashes of fury
- **Dribble Detection** (`DribbleDetectionModule`) — right — Toggle Dribble Ball detection
- **Anti-Phantom** (`PhantomModule`) — left — Anti-phantom detection
- **Manual Spam** (`Manual_Spam_Parry`) — left — Manually Spams Parry
- **Auto Spam** (`AutoSpamModule`) — right — Automatically spam parries ball
- **FOV** (`FOVModule`) — left — Changes Camera POV
- **Player Cosmetics** (`Player_Cosmetics`) — left — Apply Headless and Korblox
- **Character Speed** (``) — left — Changes Character Speed
- **Ball Stats** (`Ball_Stats`) — left — Toggle ball speed stats display
- **Custom Announcer** (`Custom_Announcer`) — left — Customize the Game Announcements
- **Hit Sounds** (`Hit_Sounds`) — right — Toggles hit sounds
- **Ability ESP** (`AbilityESPModule`) — right — Displays equipped abilities above players
- **Ability Exploit** (`AbilityExploit`) — right — Ability Exploit
- **No Render** (`No_Render`) — left — Disables Rendering of Effects
- **Semi Immortality** (`ZX_SemiImmortalMod`) — left — Click to show the floating ON/OFF panel
- **Singularity Detection** (`ZX_SingularityDetection`) — right — Blocks parry when Singularity Cape is active
- **Look at Ball** (`ZX_LookAtBall`) — left — Camera always faces the closest ball
- **Orbit Ball** (`ZX_OrbitBall`) — right — Travel to ball then orbit around it
- **Name Spoof** (`ZX_NameSpoof`) — left — Spoof your display name with verified badge
- **Atmosphere** (`ZX_WorldAtmo`) — left — Control atmosphere
- **Color Correction** (`ZX_WorldCC`) — right — Adjust colors
- **Lighting** (`ZX_WorldLight`) — right — Control lighting
- **Sky Color Override** (`ZX_SkyColor`) — left — Override sky color

## Counts
- create_module: 41
- create_checkbox: 29
- create_slider: 46
- create_dropdown: 10
- create_textbox: 4
- create_button: 2
- SendNotification: 12
